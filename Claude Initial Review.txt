<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Cherif Eddine Haddad – Growth & Marketing Specialist</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,700;0,900;1,400&family=DM+Sans:wght@300;400;500&family=DM+Mono:wght@400;500&display=swap" rel="stylesheet">
<style>
  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

  :root {
    --cream: #faf8f5;
    --terracotta: #c96a2a;
    --terracotta-light: #e8834a;
    --terracotta-dark: #a3521e;
    --brown: #2d2520;
    --brown-light: #4a3c35;
    --sage: #7a9e96;
    --sage-light: #b5ccc9;
    --amber: #d97706;
    --border: #e5dfd8;
    --muted: #9a8e86;
  }

  html { scroll-behavior: smooth; }

  body {
    font-family: 'DM Sans', sans-serif;
    background: var(--cream);
    color: var(--brown);
    font-size: 16px;
    line-height: 1.7;
    overflow-x: hidden;
  }

  /* ── NAV ───────────────────────────────────── */
  nav {
    position: sticky; top: 0; z-index: 100;
    background: rgba(250,248,245,0.96);
    backdrop-filter: blur(12px);
    border-bottom: 1px solid var(--border);
    padding: 0 clamp(1.5rem, 5vw, 4rem);
    display: flex; align-items: center; justify-content: space-between;
    height: 64px;
  }
  .nav-brand {
    font-family: 'Playfair Display', serif;
    font-size: 1.25rem; font-weight: 700;
    color: var(--brown); text-decoration: none;
    letter-spacing: -0.02em;
  }
  .nav-brand span { color: var(--terracotta); }
  .nav-links { display: flex; gap: 2.5rem; list-style: none; }
  .nav-links a {
    font-size: 0.875rem; font-weight: 500; letter-spacing: 0.03em;
    color: var(--muted); text-decoration: none;
    text-transform: uppercase;
    transition: color 0.2s;
  }
  .nav-links a:hover { color: var(--terracotta); }
  .nav-cta {
    padding: 0.5rem 1.25rem;
    background: var(--brown); color: var(--cream);
    border: none; border-radius: 100px; font-size: 0.875rem;
    font-weight: 500; cursor: pointer; text-decoration: none;
    transition: background 0.2s, transform 0.15s;
    white-space: nowrap;
  }
  .nav-cta:hover { background: var(--terracotta); transform: translateY(-1px); }

  /* ── HERO ──────────────────────────────────── */
  .hero {
    min-height: 92vh;
    display: flex; flex-direction: column; justify-content: center;
    padding: clamp(4rem, 10vw, 8rem) clamp(1.5rem, 5vw, 4rem);
    position: relative; overflow: hidden;
  }
  .hero-grid {
    position: absolute; inset: 0;
    background-image:
      linear-gradient(var(--border) 1px, transparent 1px),
      linear-gradient(90deg, var(--border) 1px, transparent 1px);
    background-size: 60px 60px;
    opacity: 0.45;
    mask-image: radial-gradient(ellipse 80% 60% at 50% 50%, black 30%, transparent 100%);
  }
  .hero-tag {
    display: inline-flex; align-items: center; gap: 0.5rem;
    font-size: 0.8rem; font-weight: 500; letter-spacing: 0.1em;
    text-transform: uppercase; color: var(--terracotta);
    background: rgba(201,106,42,0.08); border: 1px solid rgba(201,106,42,0.2);
    padding: 0.35rem 1rem; border-radius: 100px; margin-bottom: 2rem;
    width: fit-content;
  }
  .hero-tag::before { content: ''; width: 6px; height: 6px; background: var(--terracotta); border-radius: 50%; animation: pulse 2s ease-in-out infinite; }
  @keyframes pulse { 0%,100%{opacity:1;transform:scale(1)} 50%{opacity:0.5;transform:scale(0.8)} }

  .hero h1 {
    font-family: 'Playfair Display', serif;
    font-size: clamp(3rem, 7vw, 6rem);
    font-weight: 900; line-height: 1.02;
    letter-spacing: -0.035em;
    color: var(--brown);
    max-width: 14ch;
    margin-bottom: 1.75rem;
  }
  .hero h1 em { font-style: italic; color: var(--terracotta); }

  .hero-sub {
    font-size: clamp(1rem, 2vw, 1.2rem);
    color: var(--muted); max-width: 52ch;
    line-height: 1.7; margin-bottom: 2.5rem;
    font-weight: 300;
  }

  .hero-actions { display: flex; gap: 1rem; flex-wrap: wrap; }
  .btn-primary {
    display: inline-flex; align-items: center; gap: 0.5rem;
    padding: 0.875rem 2rem; background: var(--brown); color: var(--cream);
    border: none; border-radius: 100px; font-size: 0.9rem; font-weight: 500;
    text-decoration: none; cursor: pointer;
    transition: background 0.2s, transform 0.15s, box-shadow 0.2s;
  }
  .btn-primary:hover { background: var(--terracotta); transform: translateY(-2px); box-shadow: 0 8px 24px rgba(201,106,42,0.3); }
  .btn-secondary {
    display: inline-flex; align-items: center; gap: 0.5rem;
    padding: 0.875rem 2rem; background: transparent; color: var(--brown);
    border: 1.5px solid var(--border); border-radius: 100px; font-size: 0.9rem; font-weight: 500;
    text-decoration: none; cursor: pointer;
    transition: border-color 0.2s, color 0.2s, transform 0.15s;
  }
  .btn-secondary:hover { border-color: var(--terracotta); color: var(--terracotta); transform: translateY(-2px); }

  .hero-scroll {
    position: absolute; bottom: 2.5rem; left: 50%; transform: translateX(-50%);
    display: flex; flex-direction: column; align-items: center; gap: 0.5rem;
    color: var(--muted); font-size: 0.75rem; letter-spacing: 0.08em; text-transform: uppercase;
  }
  .hero-scroll-line {
    width: 1px; height: 40px; background: linear-gradient(to bottom, var(--muted), transparent);
    animation: scrollLine 1.8s ease-in-out infinite;
  }
  @keyframes scrollLine { 0%{opacity:0;transform:scaleY(0);transform-origin:top} 50%{opacity:1;transform:scaleY(1)} 100%{opacity:0;transform:scaleY(1);transform-origin:bottom} }

  /* ── METRICS BAR ───────────────────────────── */
  .metrics-bar {
    background: var(--brown);
    padding: 4rem clamp(1.5rem, 5vw, 4rem);
  }
  .metrics-grid {
    display: grid; grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
    gap: 2rem; max-width: 900px; margin: 0 auto;
    text-align: center;
  }
  .metric-item {}
  .metric-num {
    font-family: 'Playfair Display', serif;
    font-size: clamp(2.5rem, 5vw, 3.5rem); font-weight: 700;
    color: var(--cream); line-height: 1;
  }
  .metric-num span { color: var(--terracotta-light); }
  .metric-label {
    font-size: 0.8rem; color: rgba(250,248,245,0.5);
    text-transform: uppercase; letter-spacing: 0.08em;
    margin-top: 0.5rem; font-weight: 500;
  }
  .metric-divider {
    width: 1px; background: rgba(250,248,245,0.12);
    margin: 0 auto;
  }

  /* ── SECTIONS SHARED ───────────────────────── */
  section { padding: clamp(4rem, 8vw, 7rem) clamp(1.5rem, 5vw, 4rem); }
  .section-label {
    font-size: 0.75rem; letter-spacing: 0.12em; text-transform: uppercase;
    color: var(--terracotta); font-weight: 500; margin-bottom: 1rem;
  }
  h2.section-title {
    font-family: 'Playfair Display', serif;
    font-size: clamp(2rem, 4vw, 3rem); font-weight: 700;
    line-height: 1.1; letter-spacing: -0.025em;
    color: var(--brown); margin-bottom: 1.25rem;
  }
  .section-sub {
    font-size: 1.05rem; color: var(--muted);
    max-width: 52ch; line-height: 1.7; font-weight: 300;
  }

  /* ── APPROACH ──────────────────────────────── */
  .approach { background: var(--cream); }
  .approach-inner { max-width: 1100px; margin: 0 auto; }
  .approach-header { margin-bottom: 3.5rem; }
  .approach-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(240px,1fr)); gap: 1.5px; background: var(--border); border: 1px solid var(--border); border-radius: 16px; overflow: hidden; }
  .approach-card {
    background: var(--cream);
    padding: 2rem 2rem 2.5rem;
    transition: background 0.3s;
  }
  .approach-card:hover { background: #f5f1eb; }
  .approach-num {
    font-family: 'DM Mono', monospace;
    font-size: 0.75rem; color: var(--terracotta);
    letter-spacing: 0.05em; margin-bottom: 1.5rem;
  }
  .approach-icon {
    width: 40px; height: 40px;
    background: rgba(201,106,42,0.1); border-radius: 10px;
    display: flex; align-items: center; justify-content: center;
    margin-bottom: 1.25rem;
    font-size: 1.1rem;
  }
  .approach-card h3 {
    font-family: 'Playfair Display', serif;
    font-size: 1.15rem; font-weight: 700; margin-bottom: 0.75rem;
    color: var(--brown);
  }
  .approach-card p { font-size: 0.9rem; color: var(--muted); line-height: 1.65; }

  /* ── CASE STUDIES ──────────────────────────── */
  .case-studies { background: #f3efe9; }
  .case-studies-inner { max-width: 1100px; margin: 0 auto; }
  .case-header { display: flex; justify-content: space-between; align-items: flex-end; margin-bottom: 3rem; flex-wrap: wrap; gap: 1.5rem; }
  .case-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(320px, 1fr)); gap: 1.5rem; }
  .case-card {
    background: var(--cream); border-radius: 16px;
    border: 1px solid var(--border); padding: 2rem;
    transition: transform 0.3s, box-shadow 0.3s;
    display: flex; flex-direction: column;
  }
  .case-card:hover { transform: translateY(-4px); box-shadow: 0 20px 48px rgba(45,37,32,0.1); }
  .case-company-row { display: flex; align-items: center; gap: 0.75rem; margin-bottom: 1.5rem; }
  .case-company-badge {
    width: 42px; height: 42px; border-radius: 10px;
    display: flex; align-items: center; justify-content: center;
    font-size: 1rem; font-weight: 700;
    flex-shrink: 0;
  }
  .badge-orange { background: rgba(201,106,42,0.12); color: var(--terracotta); }
  .badge-blue   { background: rgba(56,106,172,0.12); color: #386aac; }
  .badge-teal   { background: rgba(122,158,150,0.18); color: #4d8078; }
  .case-company { font-size: 0.85rem; color: var(--muted); font-weight: 500; }
  .case-card h3 {
    font-family: 'Playfair Display', serif;
    font-size: 1.2rem; font-weight: 700; margin-bottom: 0.75rem; color: var(--brown);
  }
  .case-context { font-size: 0.875rem; color: var(--muted); line-height: 1.65; margin-bottom: 1.5rem; flex: 1; }
  .case-results { display: flex; flex-direction: column; gap: 0.5rem; }
  .result-item {
    display: flex; align-items: center; gap: 0.6rem;
    font-size: 0.85rem; font-weight: 500; color: var(--brown);
  }
  .result-dot { width: 6px; height: 6px; background: var(--terracotta); border-radius: 50%; flex-shrink: 0; }
  .case-scale {
    margin-top: 1.5rem; padding-top: 1.25rem; border-top: 1px solid var(--border);
    font-size: 0.8rem; color: var(--muted);
    display: flex; align-items: center; gap: 0.5rem;
  }

  /* ── PORTFOLIO GRID ────────────────────────── */
  .portfolio { background: var(--cream); }
  .portfolio-inner { max-width: 1100px; margin: 0 auto; }
  .filter-tabs { display: flex; gap: 0.5rem; flex-wrap: wrap; margin-bottom: 2.5rem; }
  .filter-tab {
    padding: 0.4rem 1.1rem; border-radius: 100px;
    font-size: 0.82rem; font-weight: 500; border: 1px solid var(--border);
    background: transparent; color: var(--muted); cursor: pointer;
    transition: all 0.2s;
  }
  .filter-tab.active, .filter-tab:hover { background: var(--brown); color: var(--cream); border-color: var(--brown); }
  .portfolio-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(300px, 1fr)); gap: 1.25rem; }
  .portfolio-item {
    border-radius: 14px; overflow: hidden; background: var(--cream);
    border: 1px solid var(--border);
    transition: transform 0.3s, box-shadow 0.3s;
    cursor: pointer;
  }
  .portfolio-item:hover { transform: translateY(-4px); box-shadow: 0 16px 40px rgba(45,37,32,0.1); }
  .portfolio-img { width: 100%; aspect-ratio: 16/9; object-fit: cover; display: block; background: #e8e2da; }
  .portfolio-body { padding: 1.25rem 1.5rem 1.5rem; }
  .portfolio-cat {
    font-size: 0.72rem; letter-spacing: 0.1em; text-transform: uppercase;
    color: var(--terracotta); font-weight: 500; margin-bottom: 0.5rem;
  }
  .portfolio-item h3 { font-family: 'Playfair Display', serif; font-size: 1.05rem; font-weight: 700; color: var(--brown); margin-bottom: 0.5rem; }
  .portfolio-item p  { font-size: 0.83rem; color: var(--muted); line-height: 1.6; margin-bottom: 1rem; }
  .portfolio-tags { display: flex; flex-wrap: wrap; gap: 0.4rem; }
  .tag {
    font-size: 0.72rem; padding: 0.25rem 0.7rem;
    background: rgba(201,106,42,0.08); color: var(--terracotta);
    border-radius: 100px; font-weight: 500;
  }
  .view-all-row { text-align: center; margin-top: 3rem; }

  /* ── EXPERTISE ─────────────────────────────── */
  .expertise { background: #f3efe9; }
  .expertise-inner { max-width: 900px; margin: 0 auto; }
  .expertise-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); gap: 1.5rem; margin-top: 3rem; }
  .expertise-card {
    background: var(--cream); border-radius: 14px; padding: 1.75rem 2rem;
    border: 1px solid var(--border);
    transition: box-shadow 0.25s;
  }
  .expertise-card:hover { box-shadow: 0 8px 32px rgba(45,37,32,0.08); }
  .expertise-card h3 {
    font-size: 0.8rem; font-weight: 500; letter-spacing: 0.08em; text-transform: uppercase;
    color: var(--muted); margin-bottom: 1rem;
  }
  .skill-pills { display: flex; flex-wrap: wrap; gap: 0.5rem; }
  .skill-pill {
    font-size: 0.825rem; padding: 0.35rem 0.85rem;
    background: rgba(201,106,42,0.07); color: var(--terracotta-dark);
    border-radius: 100px; font-weight: 500;
    border: 1px solid rgba(201,106,42,0.18);
    transition: background 0.2s;
  }
  .skill-pill:hover { background: rgba(201,106,42,0.15); }

  /* ── TESTIMONIALS ──────────────────────────── */
  .testimonials { background: var(--brown); color: var(--cream); }
  .testimonials-inner { max-width: 1100px; margin: 0 auto; }
  .testimonials .section-label { color: rgba(250,248,245,0.5); }
  .testimonials h2.section-title { color: var(--cream); }
  .testimonials-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(260px,1fr)); gap: 1.25rem; margin-top: 3rem; }
  .testimonial-card {
    background: rgba(250,248,245,0.06);
    border: 1px solid rgba(250,248,245,0.1);
    border-radius: 14px; padding: 2rem;
    transition: background 0.25s;
  }
  .testimonial-card:hover { background: rgba(250,248,245,0.1); }
  .quote-mark { font-family: 'Playfair Display', serif; font-size: 3rem; line-height: 1; color: var(--terracotta-light); margin-bottom: 1rem; }
  .testimonial-text { font-size: 0.95rem; line-height: 1.7; color: rgba(250,248,245,0.8); font-style: italic; margin-bottom: 1.5rem; font-weight: 300; }
  .testimonial-author { font-size: 0.85rem; font-weight: 600; color: var(--cream); }
  .testimonial-company { font-size: 0.78rem; color: rgba(250,248,245,0.45); margin-top: 0.25rem; }
  .testimonial-link { color: var(--terracotta-light); text-decoration: none; font-size: 0.78rem; }
  .testimonial-link:hover { text-decoration: underline; }

  /* ── ABOUT ─────────────────────────────────── */
  .about { background: var(--cream); }
  .about-inner { max-width: 1100px; margin: 0 auto; display: grid; grid-template-columns: 1fr 1fr; gap: 5rem; align-items: center; }
  .about-visual {
    background: #f3efe9; border-radius: 20px;
    padding: 3rem 2rem; text-align: center;
    border: 1px solid var(--border);
    position: relative; overflow: hidden;
  }
  .about-visual::before {
    content: '';
    position: absolute; inset: 0;
    background-image: linear-gradient(var(--border) 1px, transparent 1px), linear-gradient(90deg, var(--border) 1px, transparent 1px);
    background-size: 32px 32px; opacity: 0.4;
  }
  .about-initials {
    position: relative;
    width: 80px; height: 80px;
    background: var(--terracotta); color: var(--cream);
    border-radius: 50%; display: flex; align-items: center; justify-content: center;
    font-family: 'Playfair Display', serif; font-size: 1.75rem; font-weight: 700;
    margin: 0 auto 1.5rem;
  }
  .about-name { position: relative; font-family: 'Playfair Display', serif; font-size: 1.5rem; font-weight: 700; color: var(--brown); margin-bottom: 0.4rem; }
  .about-title { position: relative; font-size: 0.85rem; color: var(--muted); font-weight: 500; }
  .about-markets {
    position: relative; margin-top: 2rem;
    display: flex; justify-content: center; gap: 0.5rem; flex-wrap: wrap;
  }
  .market-badge {
    font-size: 0.72rem; letter-spacing: 0.06em; text-transform: uppercase;
    padding: 0.3rem 0.8rem; background: var(--cream); color: var(--brown);
    border-radius: 100px; font-weight: 600; border: 1px solid var(--border);
  }
  .about-text p { font-size: 1rem; color: var(--muted); line-height: 1.75; margin-bottom: 1.25rem; font-weight: 300; }
  .about-text p:last-child { margin-bottom: 0; }

  /* ── CTA ───────────────────────────────────── */
  .cta-section {
    background: linear-gradient(135deg, var(--terracotta) 0%, #a3521e 100%);
    text-align: center; padding: clamp(4rem, 8vw, 7rem) clamp(1.5rem, 5vw, 4rem);
  }
  .cta-section h2 {
    font-family: 'Playfair Display', serif;
    font-size: clamp(2rem, 4vw, 3rem); font-weight: 700;
    color: var(--cream); margin-bottom: 1rem; letter-spacing: -0.02em;
  }
  .cta-section p { font-size: 1.05rem; color: rgba(250,248,245,0.75); margin-bottom: 2.5rem; font-weight: 300; }
  .cta-actions { display: flex; gap: 1rem; justify-content: center; flex-wrap: wrap; }
  .btn-cta-primary {
    padding: 0.9rem 2.25rem; background: var(--cream); color: var(--terracotta);
    border-radius: 100px; font-weight: 600; font-size: 0.925rem; text-decoration: none;
    transition: transform 0.2s, box-shadow 0.2s;
  }
  .btn-cta-primary:hover { transform: translateY(-2px); box-shadow: 0 12px 32px rgba(0,0,0,0.2); }
  .btn-cta-secondary {
    padding: 0.9rem 2.25rem; background: transparent; color: var(--cream);
    border: 1.5px solid rgba(250,248,245,0.5); border-radius: 100px;
    font-weight: 500; font-size: 0.925rem; text-decoration: none;
    transition: border-color 0.2s, background 0.2s;
  }
  .btn-cta-secondary:hover { border-color: var(--cream); background: rgba(250,248,245,0.1); }

  /* ── FOOTER ────────────────────────────────── */
  footer {
    background: var(--brown); color: rgba(250,248,245,0.4);
    padding: 2rem clamp(1.5rem, 5vw, 4rem);
    display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; gap: 1rem;
    font-size: 0.82rem;
  }
  .footer-brand { font-family: 'Playfair Display', serif; color: rgba(250,248,245,0.7); font-size: 0.95rem; }

  /* ── SCROLL REVEAL ─────────────────────────── */
  .reveal { opacity: 0; transform: translateY(24px); transition: opacity 0.7s ease, transform 0.7s ease; }
  .reveal.visible { opacity: 1; transform: translateY(0); }
  .reveal-delay-1 { transition-delay: 0.1s; }
  .reveal-delay-2 { transition-delay: 0.2s; }
  .reveal-delay-3 { transition-delay: 0.3s; }
  .reveal-delay-4 { transition-delay: 0.4s; }

  /* ── SCROLL-TO-TOP ─────────────────────────── */
  #scrollTop {
    position: fixed; bottom: 2rem; right: 2rem; z-index: 99;
    width: 44px; height: 44px;
    background: var(--brown); color: var(--cream);
    border-radius: 50%; border: none; cursor: pointer;
    display: flex; align-items: center; justify-content: center;
    font-size: 1.1rem; box-shadow: 0 4px 16px rgba(45,37,32,0.25);
    opacity: 0; transform: translateY(12px);
    transition: opacity 0.3s, transform 0.3s, background 0.2s;
    pointer-events: none;
  }
  #scrollTop.show { opacity: 1; transform: translateY(0); pointer-events: all; }
  #scrollTop:hover { background: var(--terracotta); }

  /* ── MOBILE ────────────────────────────────── */
  .hamburger { display: none; flex-direction: column; gap: 5px; cursor: pointer; padding: 4px; }
  .hamburger span { width: 22px; height: 2px; background: var(--brown); border-radius: 2px; transition: all 0.3s; }
  .mobile-menu {
    display: none; position: fixed; inset: 0; z-index: 99;
    background: var(--cream); flex-direction: column;
    align-items: center; justify-content: center; gap: 2.5rem;
  }
  .mobile-menu.open { display: flex; }
  .mobile-menu a { font-family: 'Playfair Display', serif; font-size: 2rem; font-weight: 700; color: var(--brown); text-decoration: none; }
  .mobile-close { position: absolute; top: 1.5rem; right: 1.5rem; font-size: 1.5rem; cursor: pointer; background: none; border: none; color: var(--brown); }

  @media (max-width: 900px) {
    .about-inner { grid-template-columns: 1fr; gap: 2.5rem; }
    .about-visual { max-width: 360px; margin: 0 auto; }
  }
  @media (max-width: 700px) {
    .nav-links, .nav-cta { display: none; }
    .hamburger { display: flex; }
    .metric-divider { display: none; }
  }
</style>
</head>
<body>

<!-- MOBILE MENU -->
<div class="mobile-menu" id="mobileMenu">
  <button class="mobile-close" onclick="closeMobile()">✕</button>
  <a href="#about" onclick="closeMobile()">About</a>
  <a href="#work" onclick="closeMobile()">Work</a>
  <a href="#expertise" onclick="closeMobile()">Expertise</a>
  <a href="#contact" onclick="closeMobile()">Contact</a>
</div>

<!-- NAV -->
<nav>
  <a href="#" class="nav-brand">Cherif<span>.</span></a>
  <ul class="nav-links">
    <li><a href="#about">About</a></li>
    <li><a href="#work">Work</a></li>
    <li><a href="#expertise">Expertise</a></li>
    <li><a href="#contact">Contact</a></li>
  </ul>
  <a href="https://www.linkedin.com/in/cherifeddinehaddad/" target="_blank" rel="noopener" class="nav-cta">LinkedIn →</a>
  <div class="hamburger" onclick="document.getElementById('mobileMenu').classList.add('open')">
    <span></span><span></span><span></span>
  </div>
</nav>

<!-- HERO -->
<section class="hero" id="home">
  <div class="hero-grid"></div>
  <div class="hero-tag">Available for remote & consulting roles</div>
  <h1>Growth &amp; Marketing <em>Data</em> Specialist</h1>
  <p class="hero-sub">Driving acquisition, attribution, and revenue efficiency across multi-channel ecosystems — MENA, EU, and US markets.</p>
  <div class="hero-actions">
    <a href="#work" class="btn-primary">View Case Studies →</a>
    <a href="https://drive.google.com/drive/folders/1AkLIlbMkhXKsuN0knigx7flwsErfp9p6?usp=drive_link" target="_blank" class="btn-secondary">Full Portfolio</a>
  </div>
  <div class="hero-scroll">
    <div class="hero-scroll-line"></div>
    Scroll
  </div>
</section>

<!-- METRICS BAR -->
<div class="metrics-bar">
  <div class="metrics-grid">
    <div class="metric-item reveal">
      <div class="metric-num"><span id="m1">0</span>%+</div>
      <div class="metric-label">CAC Reduction</div>
    </div>
    <div class="metric-divider"></div>
    <div class="metric-item reveal reveal-delay-1">
      <div class="metric-num"><span id="m2">0</span>+</div>
      <div class="metric-label">Campaigns Managed</div>
    </div>
    <div class="metric-divider"></div>
    <div class="metric-item reveal reveal-delay-2">
      <div class="metric-num"><span id="m3">0</span>+</div>
      <div class="metric-label">Markets (MENA, EU, US)</div>
    </div>
    <div class="metric-divider"></div>
    <div class="metric-item reveal reveal-delay-3">
      <div class="metric-num">$<span id="m4">0</span>K+</div>
      <div class="metric-label">Monthly Ad Spend</div>
    </div>
  </div>
</div>

<!-- APPROACH -->
<section class="approach">
  <div class="approach-inner">
    <div class="approach-header reveal">
      <div class="section-label">How I Work</div>
      <h2 class="section-title">A Structured Approach to<br>Sustainable Growth</h2>
      <p class="section-sub">Combining data, strategy, and continuous optimization across the full marketing funnel.</p>
    </div>
    <div class="approach-grid">
      <div class="approach-card reveal">
        <div class="approach-num">01</div>
        <div class="approach-icon">📊</div>
        <h3>Tracking &amp; Attribution</h3>
        <p>GA4, GTM, Meta CAPI, and event architecture implementation to ensure accurate and reliable data across every touchpoint.</p>
      </div>
      <div class="approach-card reveal reveal-delay-1">
        <div class="approach-num">02</div>
        <div class="approach-icon">🎯</div>
        <h3>Acquisition Strategy</h3>
        <p>Channel mix optimization across Meta, Google, and TikTok with structured budget allocation and scaling frameworks.</p>
      </div>
      <div class="approach-card reveal reveal-delay-2">
        <div class="approach-num">03</div>
        <div class="approach-icon">⚡</div>
        <h3>Experimentation</h3>
        <p>Continuous A/B testing across creatives, audiences, and funnels to identify and scale the highest-performing growth opportunities.</p>
      </div>
      <div class="approach-card reveal reveal-delay-3">
        <div class="approach-num">04</div>
        <div class="approach-icon">📈</div>
        <h3>CAC &amp; LTV Optimization</h3>
        <p>Focused on reducing acquisition costs, improving ROAS, and aligning paid acquisition with long-term customer value.</p>
      </div>
    </div>
  </div>
</section>

<!-- CASE STUDIES -->
<section class="case-studies" id="work">
  <div class="case-studies-inner">
    <div class="case-header reveal">
      <div>
        <div class="section-label">Case Studies</div>
        <h2 class="section-title">Selected Work &amp; Impact</h2>
      </div>
      <p class="section-sub" style="max-width:36ch">Strategic campaigns that delivered measurable, real-world results.</p>
    </div>
    <div class="case-grid">
      <!-- Yassir -->
      <div class="case-card reveal">
        <div class="case-company-row">
          <div class="case-company-badge badge-orange">Y</div>
          <div>
            <div style="font-weight:600;font-size:.9rem;color:var(--brown)">Yassir</div>
            <div class="case-company">Ride-hailing &amp; Super App</div>
          </div>
        </div>
        <h3>App Growth – Multi-Market Acquisition</h3>
        <p class="case-context">Managed large-scale user acquisition campaigns across Algeria and expansion markets (Tunisia, Morocco, Senegal) — optimizing Meta, Google, and TikTok with data-driven creative testing.</p>
        <div class="case-results">
          <div class="result-item"><div class="result-dot"></div>Reduced CAC by 50%+ across multiple GEOs</div>
          <div class="result-item"><div class="result-dot"></div>Scaled installs and in-app engagement</div>
          <div class="result-item"><div class="result-dot"></div>Improved attribution accuracy &amp; efficiency</div>
        </div>
        <div class="case-scale">🌍 Algeria · Tunisia · Morocco · Senegal</div>
      </div>
      <!-- HIS -->
      <div class="case-card reveal reveal-delay-1">
        <div class="case-company-row">
          <div class="case-company-badge badge-blue">H</div>
          <div>
            <div style="font-weight:600;font-size:.9rem;color:var(--brown)">HIS University</div>
            <div class="case-company">Higher Education</div>
          </div>
        </div>
        <h3>Lead Generation &amp; Student Acquisition</h3>
        <p class="case-context">Built full-funnel acquisition system across Meta, Google, and LinkedIn — including landing page optimization and email marketing flows for a private university.</p>
        <div class="case-results">
          <div class="result-item"><div class="result-dot"></div>Increased qualified leads by 30%</div>
          <div class="result-item"><div class="result-dot"></div>Reduced CPL through funnel optimization</div>
          <div class="result-item"><div class="result-dot"></div>Strengthened inbound via SEO &amp; email</div>
        </div>
        <div class="case-scale">🎓 National multi-channel funnel</div>
      </div>
      <!-- CSA -->
      <div class="case-card reveal reveal-delay-2">
        <div class="case-company-row">
          <div class="case-company-badge badge-teal">C</div>
          <div>
            <div style="font-weight:600;font-size:.9rem;color:var(--brown)">CSA Research</div>
            <div class="case-company">B2B Research · USA</div>
          </div>
        </div>
        <h3>B2B Marketing Operations</h3>
        <p class="case-context">Supported B2B marketing operations for a US-based research company — implementing HubSpot workflows, segmentation, and LinkedIn content strategy.</p>
        <div class="case-results">
          <div class="result-item"><div class="result-dot"></div>Increased LinkedIn content output by 200%</div>
          <div class="result-item"><div class="result-dot"></div>Improved lead nurturing efficiency</div>
          <div class="result-item"><div class="result-dot"></div>Enhanced reporting &amp; campaign visibility</div>
        </div>
        <div class="case-scale">🇺🇸 B2B international · US market</div>
      </div>
    </div>
  </div>
</section>

<!-- PORTFOLIO GRID -->
<section class="portfolio">
  <div class="portfolio-inner">
    <div class="reveal" style="margin-bottom:2rem">
      <div class="section-label">Portfolio</div>
      <h2 class="section-title">Campaign Highlights</h2>
    </div>
    <div class="filter-tabs reveal" id="filterTabs">
      <button class="filter-tab active" data-cat="all">All</button>
      <button class="filter-tab" data-cat="Paid Marketing">Paid Marketing</button>
      <button class="filter-tab" data-cat="Strategy & Analytics">Analytics</button>
      <button class="filter-tab" data-cat="Creative Strategy">Creative</button>
    </div>
    <div class="portfolio-grid" id="portfolioGrid">
      <!-- Items injected by JS -->
    </div>
    <div class="view-all-row reveal">
      <a href="https://drive.google.com/drive/folders/1AkLIlbMkhXKsuN0knigx7flwsErfp9p6?usp=drive_link" target="_blank" class="btn-secondary">View Full Portfolio →</a>
    </div>
  </div>
</section>

<!-- EXPERTISE -->
<section class="expertise" id="expertise">
  <div class="expertise-inner">
    <div class="reveal">
      <div class="section-label">Skills &amp; Tools</div>
      <h2 class="section-title">Expertise</h2>
      <p class="section-sub">Focused on acquisition strategy, marketing analytics, and scalable growth systems.</p>
    </div>
    <div class="expertise-grid">
      <div class="expertise-card reveal">
        <h3>Paid Advertising</h3>
        <div class="skill-pills">
          <span class="skill-pill">Meta Ads</span>
          <span class="skill-pill">Google Ads</span>
          <span class="skill-pill">TikTok Ads</span>
          <span class="skill-pill">LinkedIn Ads</span>
          <span class="skill-pill">Snapchat Ads</span>
          <span class="skill-pill">DV360</span>
          <span class="skill-pill">Reddit Ads</span>
        </div>
      </div>
      <div class="expertise-card reveal reveal-delay-1">
        <h3>Analytics &amp; Tracking</h3>
        <div class="skill-pills">
          <span class="skill-pill">GA4</span>
          <span class="skill-pill">Google Tag Manager</span>
          <span class="skill-pill">Meta CAPI</span>
          <span class="skill-pill">Attribution Modeling</span>
          <span class="skill-pill">Looker Studio</span>
        </div>
      </div>
      <div class="expertise-card reveal reveal-delay-2">
        <h3>Growth &amp; Optimization</h3>
        <div class="skill-pills">
          <span class="skill-pill">A/B Testing</span>
          <span class="skill-pill">Funnel Optimization</span>
          <span class="skill-pill">User Acquisition</span>
          <span class="skill-pill">Retention Strategy</span>
          <span class="skill-pill">CAC Reduction</span>
        </div>
      </div>
      <div class="expertise-card reveal reveal-delay-3">
        <h3>CRM &amp; Automation</h3>
        <div class="skill-pills">
          <span class="skill-pill">HubSpot</span>
          <span class="skill-pill">GoHighLevel</span>
          <span class="skill-pill">Mailchimp</span>
          <span class="skill-pill">Zapier</span>
          <span class="skill-pill">ClickUp</span>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- TESTIMONIALS -->
<section class="testimonials">
  <div class="testimonials-inner">
    <div class="reveal">
      <div class="section-label">Social Proof</div>
      <h2 class="section-title">What Clients Say</h2>
    </div>
    <div class="testimonials-grid">
      <div class="testimonial-card reveal">
        <div class="quote-mark">"</div>
        <p class="testimonial-text">Chérif combines strategic thinking with strong execution. His work directly improved our visibility and client acquisition in AI-driven solutions.</p>
        <div class="testimonial-author">We Better AI</div>
        <a href="https://www.webetterai.com/en" target="_blank" class="testimonial-link">webetterai.com ↗</a>
      </div>
      <div class="testimonial-card reveal reveal-delay-1">
        <div class="quote-mark">"</div>
        <p class="testimonial-text">Organized, efficient, and creative. He quickly adapts to new technologies and plays a key role in executing digital marketing campaigns.</p>
        <div class="testimonial-author">CSA Research</div>
        <a href="https://csa-research.com" target="_blank" class="testimonial-link">csa-research.com ↗</a>
      </div>
      <div class="testimonial-card reveal reveal-delay-2">
        <div class="quote-mark">"</div>
        <p class="testimonial-text">Strong expertise in paid ads and analytics. Reliable, proactive, and consistently delivers quality results with a great team spirit.</p>
        <div class="testimonial-author">HIS University</div>
        <a href="https://his.edu.dz" target="_blank" class="testimonial-link">his.edu.dz ↗</a>
      </div>
    </div>
  </div>
</section>

<!-- ABOUT -->
<section class="about" id="about">
  <div class="about-inner">
    <div class="about-visual reveal">
      <div class="about-initials">CE</div>
      <div class="about-name">Cherif Eddine Haddad</div>
      <div class="about-title">Growth &amp; Marketing Data Specialist</div>
      <div class="about-markets">
        <span class="market-badge">🇩🇿 Algeria</span>
        <span class="market-badge">🌍 MENA</span>
        <span class="market-badge">🇪🇺 Europe</span>
        <span class="market-badge">🇺🇸 US</span>
      </div>
    </div>
    <div class="about-text reveal reveal-delay-1">
      <div class="section-label">About Me</div>
      <h2 class="section-title" style="font-size:clamp(1.75rem,3.5vw,2.5rem)">5+ Years Building<br>Scalable Growth Systems</h2>
      <p>Performance Marketing &amp; Growth Specialist with deep expertise in multi-channel acquisition across Meta, Google, TikTok, and LinkedIn. Proven track record in scaling user acquisition, reducing CAC, and improving ROI through data-driven experimentation.</p>
      <p>I focus on building and scaling acquisition systems that drive sustainable growth — not just short-term performance. My approach combines data, experimentation, and structured execution across the full funnel.</p>
      <p>I work with startups and established companies across MENA, EU, and US markets to improve marketing efficiency, align acquisition with business goals, and create scalable growth frameworks.</p>
      <div style="margin-top:2rem;display:flex;gap:1rem;flex-wrap:wrap">
        <a href="https://www.linkedin.com/in/cherifeddinehaddad/" target="_blank" class="btn-primary">LinkedIn Profile →</a>
        <a href="mailto:cherifedd@gmail.com" class="btn-secondary">Send Email</a>
      </div>
    </div>
  </div>
</section>

<!-- CTA -->
<section class="cta-section" id="contact">
  <div class="reveal">
    <h2>Let's Build Something<br>That Scales</h2>
    <p>Open to remote roles, consulting, and growth-focused projects across international markets.</p>
    <div class="cta-actions">
      <a href="https://www.linkedin.com/in/cherifeddinehaddad/" target="_blank" class="btn-cta-primary">Connect on LinkedIn</a>
      <a href="mailto:cherifedd@gmail.com" class="btn-cta-secondary">cherifedd@gmail.com</a>
    </div>
  </div>
</section>

<!-- FOOTER -->
<footer>
  <div class="footer-brand">Cherif Eddine Haddad</div>
  <div>© 2026 · All rights reserved</div>
</footer>

<!-- SCROLL TO TOP -->
<button id="scrollTop" onclick="window.scrollTo({top:0,behavior:'smooth'})">↑</button>

<script>
  // ── Portfolio data ──
  const portfolioItems = [
    { id:'google-ads', title:'Ecommerce – Google Ads Optimization', cat:'Paid Marketing', img:'https://d2xsxph8kpxj0f.cloudfront.net/310419663030567616/2YhBxqd8JaL2MQs9Zmtjnk/Google%20Ads%20Ecommerce_a7953edc.jpg', desc:'Optimized high-spend Google Ads campaigns with advanced conversion tracking and audience targeting.', tags:['Google Ads','GA4'] },
    { id:'meta-liwa', title:'App Marketing – Meta Ads Growth', cat:'Paid Marketing', img:'https://d2xsxph8kpxj0f.cloudfront.net/310419663030567616/2YhBxqd8JaL2MQs9Zmtjnk/Meta%20Liwa%20Portfolio_c18c327a.png', desc:'Managed high-budget Meta campaigns for app user acquisition. 61+ campaigns, 151K+ impressions.', tags:['Meta Ads','TikTok','Snapchat'] },
    { id:'real-estate', title:'Real Estate Lead Generation', cat:'Paid Marketing', img:'https://d2xsxph8kpxj0f.cloudfront.net/310419663030567616/2YhBxqd8JaL2MQs9Zmtjnk/RealEstate_Meta_Campaigns_20c5db5b.webp', desc:'816 total leads at DZD 167–191 CPL across DZD 125K+ in total spend.', tags:['Meta Ads','CRM'] },
    { id:'linkedin', title:'LinkedIn B2B Acquisition', cat:'Paid Marketing', img:'https://d2xsxph8kpxj0f.cloudfront.net/310419663030567616/2YhBxqd8JaL2MQs9Zmtjnk/LinkedIn%20Ads%20%28CSA%29_f42b9310.png', desc:'B2B lead generation via LinkedIn with strategic audience targeting for high-value prospects.', tags:['LinkedIn Ads'] },
    { id:'looker', title:'Looker Studio Analytics Dashboard', cat:'Strategy & Analytics', img:'https://d2xsxph8kpxj0f.cloudfront.net/310419663030567616/2YhBxqd8JaL2MQs9Zmtjnk/Report%20Looker%20Studio_537576ee.png', desc:'Comprehensive multi-channel analytics dashboards with real-time KPI reporting.', tags:['Looker Studio','GA4'] },
    { id:'hubspot', title:'HubSpot Marketing Automation', cat:'Strategy & Analytics', img:'https://d2xsxph8kpxj0f.cloudfront.net/310419663030567616/2YhBxqd8JaL2MQs9Zmtjnk/HubSpot_Report_Updated_d2b4254b.png', desc:'97.5% delivery rate, 0.67% hard bounce rate — full segmentation & workflow implementation.', tags:['HubSpot'] },
    { id:'creative', title:'Creative Strategy & Ad Development', cat:'Creative Strategy', img:'https://d2xsxph8kpxj0f.cloudfront.net/310419663030567616/2YhBxqd8JaL2MQs9Zmtjnk/Creatives%20Approach%20_6b2cab67.png', desc:'Performance-driven creative frameworks balancing brand messaging with measurable results.', tags:['Meta','Google Ads'] },
  ];

  function renderPortfolio(cat) {
    const grid = document.getElementById('portfolioGrid');
    const items = cat === 'all' ? portfolioItems : portfolioItems.filter(i => i.cat === cat);
    grid.innerHTML = items.map(item => `
      <div class="portfolio-item">
        <img class="portfolio-img" src="${item.img}" alt="${item.title}" loading="lazy" onerror="this.style.background='#e8e2da'">
        <div class="portfolio-body">
          <div class="portfolio-cat">${item.cat}</div>
          <h3>${item.title}</h3>
          <p>${item.desc}</p>
          <div class="portfolio-tags">${item.tags.map(t=>`<span class="tag">${t}</span>`).join('')}</div>
        </div>
      </div>
    `).join('');
  }
  renderPortfolio('all');

  document.getElementById('filterTabs').addEventListener('click', e => {
    if (!e.target.classList.contains('filter-tab')) return;
    document.querySelectorAll('.filter-tab').forEach(b => b.classList.remove('active'));
    e.target.classList.add('active');
    renderPortfolio(e.target.dataset.cat);
  });

  // ── Scroll reveal ──
  const observer = new IntersectionObserver(entries => {
    entries.forEach(e => { if (e.isIntersecting) { e.target.classList.add('visible'); observer.unobserve(e.target); } });
  }, { threshold: 0.12 });
  document.querySelectorAll('.reveal').forEach(el => observer.observe(el));

  // ── Animated counters ──
  function animCount(el, target, duration) {
    let start = 0, startTime = null;
    function step(ts) {
      if (!startTime) startTime = ts;
      const progress = Math.min((ts - startTime) / duration, 1);
      el.textContent = Math.floor(progress * target);
      if (progress < 1) requestAnimationFrame(step);
      else el.textContent = target;
    }
    requestAnimationFrame(step);
  }
  const counterObs = new IntersectionObserver(entries => {
    entries.forEach(e => {
      if (e.isIntersecting) {
        animCount(document.getElementById('m1'), 20, 1800);
        animCount(document.getElementById('m2'), 100, 1800);
        animCount(document.getElementById('m3'), 4, 1200);
        animCount(document.getElementById('m4'), 80, 1800);
        counterObs.disconnect();
      }
    });
  }, { threshold: 0.4 });
  counterObs.observe(document.querySelector('.metrics-bar'));

  // ── Scroll-to-top button ──
  const scrollBtn = document.getElementById('scrollTop');
  window.addEventListener('scroll', () => {
    scrollBtn.classList.toggle('show', window.scrollY > 500);
  });

  // ── Mobile menu close ──
  function closeMobile() { document.getElementById('mobileMenu').classList.remove('open'); }
</script>
</body>
</html>